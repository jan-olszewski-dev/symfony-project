{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
        {{ form_errors(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Send"/>
    {{ form_end(form) }}
    <a href="{{ path('app_google_login') }}">Sign in by Google</a>
{% endblock %}
