{% extends 'base.html.twig' %}

{% block body %}
    {% block form %}
        <div class="form-wrapper">
            {{ form_start(form) }}
            {{ form_errors(form) }}
            {{ form_widget(form) }}
            <input class="btn btn-primary d-block ml-auto mr-0" type="submit" value="Create">
            {{ form_end(form) }}
        </div>
    {% endblock %}
{% endblock %}
