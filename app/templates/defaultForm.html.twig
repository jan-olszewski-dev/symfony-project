{% extends 'base.html.twig' %}

{% block body %}
    <div class="form-wrapper">
        {{ form_start(form) }}
        {{ form_errors(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="create"/>
        {{ form_end(form) }}
    </div>
{% endblock %}
