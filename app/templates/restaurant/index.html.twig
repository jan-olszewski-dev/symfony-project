{% extends 'base.html.twig' %}

{% block body %}
    {% for restaurant in restaurants %}
        <div>
            <h2>{{ restaurant.name }}</h2>
            <a class="btn btn-info" href="{{ path('app_restaurant_info', {'id': restaurant.id}) }}">more info</a>
        </div>
    {% else %}
        <p>No restaurants available</p>
    {% endfor %}
{% endblock %}
